. {
    log
    errors
    reload
    whoami
    prometheus
    
    hosts {
        import /etc/coredns/open.y2e.org.hosts
        import /etc/coredns/tun.y2e.org.hosts
        fallthrough
    }
    
    forward . 1.1.1.1
}
